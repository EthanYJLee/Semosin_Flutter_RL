import 'package:firebase_auth/firebase_auth.dart';

class FirebaseAuth {
  /// 날짜 :2023.03.13
  /// 작성자 : 권순형
  /// 만든이 :
  /// 내용 : firebase auth에 해당 이메일, 비밀번호 있는지 확인하기
  /// 비고 : firebase auth가 아니라 firebase firestore에서 찾아야 될지도 모름
  login() async {
    // firebase auth에서 매개변수로 받은 해당 이메일과 비밀번호 있는지 확인
    // 있는지 없는지 값 true false로 보내기
  }
}
